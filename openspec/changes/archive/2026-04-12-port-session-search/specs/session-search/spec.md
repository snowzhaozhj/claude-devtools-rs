<!--
  session-search capability 的现有 spec 已覆盖全部核心行为（followups.md 标记 ✅ 行为全对）。
  SSH stage-limit 的 coverage-gap 已在 baseline 归档时补入 spec（Requirement:
  "Support staged-limit search over SSH contexts"）。

  本 port change 按现有 spec 实现，无需 ADDED / MODIFIED / REMOVED 任何 requirement。
-->

（此 capability port 无 spec 变更——按现有 `openspec/specs/session-search/spec.md` 实现即可）

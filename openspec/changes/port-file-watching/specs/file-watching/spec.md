<!-- 
  file-watching capability 的现有 spec（openspec/specs/file-watching/spec.md）与 TS 实现
  完全匹配（followups.md ✅ 完全匹配，无 impl-bug）。本 port change 直接按现有 spec 实现，
  无需 ADDED / MODIFIED / REMOVED 任何 requirement。
  
  此文件作为占位符，表明已审查 spec delta 并确认无变更。
-->

（此 capability port 无 spec 变更——按现有 `openspec/specs/file-watching/spec.md` 实现即可）
